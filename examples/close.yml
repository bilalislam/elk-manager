actions:
  1:
    action: close
    description: >-
      Close indices older than 0  days (based on index name), for logstash-
      prefixed indices.
    options:
      disable_action: False
    filters:
    - filtertype: pattern
      kind: prefix
      value: logstash-2017.11.26
